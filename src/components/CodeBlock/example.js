export const exampleCode = `import Example from './example.js'

export const Example = () => {

    function handleExample(foo){
        let bar = foo + foo

        return bar
    }

    return (
        <div onMouseLeave={handleExample}>
            Write examples here etc.
        </div>
    )
}

export default Example;
`;
